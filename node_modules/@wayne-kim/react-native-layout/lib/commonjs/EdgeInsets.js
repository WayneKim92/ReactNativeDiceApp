"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.EdgeInsets = void 0;
const fromLTRB = (left, top, right, bottom) => {
  const paddingLeft = left;
  const paddingTop = top;
  const paddingRight = right;
  const paddingBottom = bottom;
  return {
    paddingLeft: paddingLeft !== 0 ? paddingLeft : undefined,
    paddingTop: paddingTop !== 0 ? paddingTop : undefined,
    paddingRight: paddingRight !== 0 ? paddingRight : undefined,
    paddingBottom: paddingBottom !== 0 ? paddingBottom : undefined
  };
};
const fromVH = (vertical, horizontal) => fromLTRB(horizontal, vertical, horizontal, vertical);
const all = inset => fromLTRB(inset, inset, inset, inset);
const left = inset => fromLTRB(inset, 0, 0, 0);
const top = inset => fromLTRB(0, inset, 0, 0);
const right = inset => fromLTRB(0, 0, inset, 0);
const bottom = inset => fromLTRB(0, 0, 0, inset);
const vertical = inset => fromLTRB(0, inset, 0, inset);
const horizontal = inset => fromLTRB(inset, 0, inset, 0);
const EdgeInsets = exports.EdgeInsets = {
  fromLTRB,
  fromVH,
  all,
  left,
  top,
  right,
  bottom,
  vertical,
  horizontal
};
//# sourceMappingURL=EdgeInsets.js.map